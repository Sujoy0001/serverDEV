[project]
name = "server"
version = "0.1.0"
description = "FastAPI backend with nginx reverse proxy"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111.0",
    "pydantic>=2.7.0",
    "uvicorn>=0.30.0",
]
